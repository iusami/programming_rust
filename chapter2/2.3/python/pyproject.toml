[tool.poetry]
name = "python"
version = "0.1.0"
description = ""
authors = ["iusami <ippei@hacarus.com>"]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^7"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
